services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - game-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0

volumes:
  game-data:
